name: Refresh authorized auction data

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-authorized-auction-data
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Refresh due auction sources
        env:
          BIDAI_APIFY_DATASET_ID: ${{ secrets.BIDAI_APIFY_DATASET_ID }}
          BIDAI_APIFY_DATASET_IDS: ${{ secrets.BIDAI_APIFY_DATASET_IDS }}
          BIDAI_APIFY_TOKEN: ${{ secrets.BIDAI_APIFY_TOKEN }}
          BIDAI_FEED_URL: ${{ secrets.BIDAI_FEED_URL }}
          BIDAI_FEED_URLS: ${{ secrets.BIDAI_FEED_URLS }}
          BIDAI_SOURCE_CONFIG_JSON: ${{ secrets.BIDAI_SOURCE_CONFIG_JSON }}
          BIDAI_SOURCE_AUTHORIZED: ${{ secrets.BIDAI_SOURCE_AUTHORIZED }}
        run: node scripts/refresh-all-sources.mjs

      - name: Commit changed snapshot data
        shell: bash
        run: |
          if [[ ! -f data/live-snapshots.js ]]; then
            echo "No generated snapshot file; nothing to commit."
            exit 0
          fi

          git add -- data/live-snapshots.js
          if git diff --cached --quiet; then
            echo "Authorized feed data is unchanged."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore(data): refresh auction snapshots"
          git push origin HEAD:main
